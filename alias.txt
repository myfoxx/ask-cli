# Alias per Ollama CLI helper
alias askcode='ask -m qwen3.5:latest'   # Code/debug (mid-size, --think=false)
alias askhere='ask -c'                  # Context aware
alias askerr='ask -e'                   # Error analysis wrapper
alias askv='ask -v'                     # Verbose mode
alias askfast='ask -f'                  # Instant answer (gemma3:1b)
alias askarchive='ask -a -s ~/.ask-history'  # Append answers to history file
